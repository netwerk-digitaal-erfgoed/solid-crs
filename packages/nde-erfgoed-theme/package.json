{
  "name": "@digita-ai/nde-erfgoed-theme",
  "version": "0.0.1",
  "description": "Collection of Semantic Components",
  "author": "Wouter Termont <wouter.termont@digita.ai>",
  "main": "./dist/theme.css",
  "module": "./dist/theme.css",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf ./dist && esbuild lib/theme.css --bundle --outdir=dist --minify",
    "build:dev": "rm -rf ./dist && esbuild lib/theme.css --bundle --outdir=dist",
    "lint": "stylelint \"lib/**/*.css\"",
    "lint:fix": "stylelint \"lib/**/*.css\" --fix"
  },
  "devDependencies": {
    "esbuild": "^0.11.6",
    "stylelint": "^13.12.0",
    "stylelint-config-standard": "^21.0.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/digita-ai/nde-erfgoed.git",
    "directory": "packages/nde-erfgoed-theme"
  },
  "bugs": {
    "url": "https://github.com/digita-ai/nde-erfgoed/issues"
  }
}
