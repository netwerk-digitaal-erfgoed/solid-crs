<h1>U bent aangemeld</h1>

<% if (createPod) { %>
  <h2>Uw nieuwe Pod</h2>
  <p>
    Uw nieuwe Pod staat op <a href="<%= podBaseUrl %>" class="link"><%= podBaseUrl %></a>.
    <br>
    U kunt uw documenten en gegevens daar opslaan.
  </p>
<% } %>

<% if (createWebId) { %>
  <h2>Uw nieuwe WebID</h2>
  <p>
    Je nieuwe WebID is <a href="<%= webId %>" class="link"><%= webId %></a>.
    <br>
    U kunt deze ID gebruiken om te communiceren met Solid pods en apps.
  </p>
<% } %>

<% if (register) { %>
  <h2>Uw nieuwe account</h2>
  <p>
    Via je e-mailadres <em><%= email %></em>,
    <% if (authenticating) { %>
      kan je nu <a href="./login">inloggen</a>
    <% } else { %>
        kun je op deze server inloggen op Solid apps
    <% } %>
    met je WebID <a href="<%= webId %>" class="link"><%= webId %></a>
  </p>
  <% if (!createWebId) { %>
    <p>
    U moet de triple toevoegen
      <code><%= `<${webId}> <http://www.w3.org/ns/solid/terms#oidcIssuer> <${oidcIssuer}>.`%></code>
      aan uw bestaande WebID-document <em><%= webId %></em> om aan te geven dat u deze server vertrouwt als inlogprovider.
  <% } %>
<% } %>